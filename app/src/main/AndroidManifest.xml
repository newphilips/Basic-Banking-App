<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.basicbankingapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BasicBankingApp">
        <activity android:name=".ui.AllTransactions">
        </activity>
        <activity android:name=".ui.SuccessfulTransaction" />
        <activity android:name=".ui.SelectUser" />
        <activity android:name=".ui.AllUsersActivity" />
        <activity android:name=".ui.CustomerDetails" />
        <activity android:name=".ui.SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.MainActivity"
            android:theme="@style/MainActivityTheme"/>
    </application>

</manifest>